'use strict'

const Editor = require('Editor')
const EditorState = require('EditorState')
const Decorator = require('Decorator')
const sfd = {
  Editor,
  EditorState,
  Decorator
}

module.exports = sfd
